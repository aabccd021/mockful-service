{
  "name": "netero-oauth-mock",
  "scripts": {
    "postinstall": "cd $(dirname $npm_package_json) && nix run --inputs-from . github:baileyluTCD/bun2nix -- --output-file ./bun.nix"
  },
  "dependencies": {
    "@types/bun": "latest",
    "jose": "latest"
  }
}
