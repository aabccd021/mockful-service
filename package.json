{
  "name": "netero-oauth-mock",
  "scripts": {
    "build": "bun build ./src/index.ts --compile --bytecode --sourcemap --outfile ./dist/netero-oauth-mock",
    "format": "bunx biome format --error-on-warnings --indent-style=space --line-width=100",
    "lint": "bunx biome lint --error-on-warnings"
  },
  "dependencies": {
    "@types/bun": "latest",
    "openid-client": "latest"
  }
}
