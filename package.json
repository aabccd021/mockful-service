{
  "name": "netero-oauth-mock",
  "scripts": {
    "postinstall": "nix run --inputs-from . github:baileyluTCD/bun2nix -- --output-file ./bun.nix",
    "build": "bun build ./src/index.ts --compile --minify --sourcemap --bytecode --outfile ./dist/server"
  },
  "dependencies": {
    "@types/bun": "latest",
    "openid-client": "^6.6.4"
  }
}
