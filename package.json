{
  "name": "netero-oauth-mock",
  "scripts": {
    "postinstall": "cd $(dirname $npm_package_json) && nix run .#bun2nix -- --output-file ./bun.nix"
  },
  "dependencies": {
    "@types/bun": "latest",
    "jose": "latest",
    "superstruct": "latest"
  }
}
