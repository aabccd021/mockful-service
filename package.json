{
  "name": "netero-oauth-mock",
  "scripts": {
    "build": "bun build ./src/index.ts --compile --bytecode --sourcemap --outfile ./dist/netero-oauth-mock",
    "format": "biome format --error-on-warnings --indent-style=space --line-width=100",
    "lint": "biome lint --error-on-warnings",
    "check": "tsc"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "biome": "latest",
    "openid-client": "latest",
    "typescript": "latest"
  }
}
